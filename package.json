{
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "develop": "concurrently \"cd server && npm run watch\" \"cd merch-guild-native && npm run web\"",
    "install": "cd server && npm i && cd ../merch-guild-native && npm i",
    "build": "cd merch-guild-native && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.1.0"
  }
}